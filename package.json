{
  "name": "wepy-plugin-minifier",
  "version": "1.1.0",
  "description": "'wepy minifier plugin'",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && babel src -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "minifier",
    "wepy-plugin"
  ],
  "author": "hukaibaihu",
  "license": "MIT",
  "dependencies": {
    "pretty-data": "^0.40.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
